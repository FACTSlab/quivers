# Objectives

Variational objectives include `ELBO`, `IWAEBound`, `RenyiBound`,
`VRIWAEBound`, `ChiVI`, `RWS`, and `DReGsBound`. They pair with the
gradient-estimator strategies in [Estimators](estimators.md).

::: quivers.inference.objectives
