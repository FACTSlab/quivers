# Objectives

Variational objectives: `ELBO`, `IWAEBound`, `RenyiBound`, `VRIWAEBound`, `ChiVI`, `RWS`, and `DReGsBound`, paired with the gradient-estimator strategies in [Estimators](estimators.md).

::: quivers.inference.objectives
