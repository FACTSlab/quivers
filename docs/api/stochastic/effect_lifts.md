# Effect Schema Lifting

`class_directed_lifts(base_schema, effect)` and
`lift_rule_set(base_schemas, effects)` emit lifted `SchemaDecl`s for
the typeclasses inhabited by an effect.

::: quivers.stochastic.effect_lifts
