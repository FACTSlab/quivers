# Exercises LossDecl (training loss attached to a learnable element).
object Resp : FinSet 4
program prog : Resp -> Resp
    sample theta <- Beta(2.0, 2.0)
    observe y <- Bernoulli(theta)
    return theta
loss negloglik [weight=1.0, on=program(prog)]
    -log_prob_sum
export prog
