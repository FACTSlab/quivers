# Exercises RuleDecl (typed inference rule at the top level).
category S, NP, VP
rule combine(X, Y) : NP, VP => S
object Resp : FinSet 4
program prog : Resp -> Resp
    sample x <- Bernoulli(0.5)
    return x
export prog
