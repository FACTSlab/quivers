# Exercises LetExprLambda (`param -> body` inside a fold-style call).
object Obs : FinSet 4
program prog : Obs -> Obs
    sample x <- Normal(0.0, 1.0)
    let doubled = (x_in -> x_in + x_in)(x)
    observe y <- Normal(doubled, 1.0)
    return doubled
export prog
