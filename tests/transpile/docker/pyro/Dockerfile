# Pyro probe image: pyro-ppl + torch.
# Tag: panproto-test-pyro
FROM python:3.12-slim

RUN pip install --no-cache-dir \
    pyro-ppl==1.9.1 \
    torch==2.5.1 \
    numpy==1.26.4

ENTRYPOINT ["python"]
